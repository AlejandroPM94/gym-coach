services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: gym_coach
      POSTGRES_USER: gym_coach
      POSTGRES_PASSWORD: gym_coach
    ports:
      - "5432:5432"
    volumes:
      - gym_coach_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gym_coach -d gym_coach"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  gym_coach_postgres:

